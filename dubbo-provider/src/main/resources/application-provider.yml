dubbo:
  version: 1.0.0
  application:
    id: spring-provider
    #给provider起名称
    name: dubbo-provider
  protocol:
    #dubbo协议
    name: dubbo
    #端口
    port: 20880
  scan:
    base-packages: top.inson.dubbo.service
  #提供者配置
  provider:
    version: 1.0.0
    timeout: 30000
